# Copy to frontend/.env and set as needed. frontend/.env is gitignored and never committed.
# Vite auto-loads this file at build time (npm run build / build:dev).

# Dev mode: when true, features registered with "dev" in features.ts show on this frontend.
# When false (or unset), only features registered as true show.
VITE_DEV_MODE=true